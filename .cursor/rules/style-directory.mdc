---
description: When generating file materials, follow style/ directory for tone, structure, and examples
alwaysApply: true
---

# 风格目录（生成材料时强制参照）

1. **范围**：凡在本仓库中**新建或改写**面向交付的文件材料（如 Markdown/Word 文案、报告结构、PPT 大纲与逐字稿、表格配套说明、邮件/纪要模板等），须将 **`style/`** 作为版式与表述的**首要参照**。
2. **执行顺序**：生成前先**浏览并阅读** `style/` 下与用户任务相关的文件（规范类 `.md`、以及 `style/samples/` 等范例）；若用户指定仅使用部分文件，以用户指定为准。
3. **对齐内容**：在材料中落实风格目录中的**结构层级、术语、语气、章节习惯、列表与标题格式**；若有范例，**优先对齐范例的组织方式**，而非自行发明另一套版式。
4. **与事实约束的关系**：`style/` 只约束**怎么写、长什么样**；**事实、数据、公式与口径**仍须遵守工作材料规程（以 `materials/` 与共识文件为据），**不得**用风格范例中的示例数字替代真实材料中的数据。
5. **目录为空或缺失**：若 `style/` 中尚无适用文件，应说明「当前无风格参照文件」，并采用**中性、专业**的默认结构；同时可建议用户向 `style/` 补充 `writing-guide.md` 或样例。

风格目录说明：[style/README.md](../../style/README.md)
